name: Generate and Send Load Test Email
 
on:
  # Trigger on schedule (e.g., every Monday at 9 AM)
  schedule:
    - cron: '0 9 * * 1'
  # Manual trigger
  workflow_dispatch:
  # Trigger when CSV files are pushed
  push:
    paths:
      - 'csv_files/**'
 
jobs:
  generate-and-send-email:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install python-dotenv
      - name: Generate email from CSV
        run: |
          python scripts/generate_email.py
      - name: Create environment file
        env:
          SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SENDER_EMAIL: ${{ secrets.SENDER_EMAIL }}
          SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD }}
        run: |
          cat > config/.env << EOF
          SMTP_SERVER=$SMTP_SERVER
          SMTP_PORT=$SMTP_PORT
          SENDER_EMAIL=$SENDER_EMAIL
          SENDER_PASSWORD=$SENDER_PASSWORD
          SMTP_USE_TLS=true
          EOF
      - name: Send email
        run: |
          python scripts/send_email.py
      - name: Upload email artifact
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: generated-email
          path: output/email_summary.html
